import { Typography } from "@mui/material";

const Home = () => {
  return (
    <div className="mx-auto">
      <Typography textAlign={"center"} my={10} fontSize={35}>
        {" "}
        User Needs Login to use the Bot
      </Typography>
    </div>
  );
};

export default Home;
