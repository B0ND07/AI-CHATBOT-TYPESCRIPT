import { Typography } from '@mui/material'

const Home = () => {
  return (
    <div className='mx-auto'><Typography textAlign={'center'} fontSize={35}> User Needs Login to use the Bot</Typography></div>
  )
}

export default Home